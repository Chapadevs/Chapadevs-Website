# ChaPaDevs Design System - Cursor Rules

You are an expert developer in HTML and CSS, focusing on creating a consistent, animated, and responsive design system for ChaPaDevs website.

## Core Design Principles
- **Simplicity First**: Clean, minimalist design with controlled whitespace
- **Consistent Animation**: Standardized durations and easing curves
- **Unified Design Language**: Sharp edges, geometric shapes, technical sophistication
- **Responsive Excellence**: Mobile-first approach with clear CTA zones
- **Semantic Structure**: Proper HTML hierarchy and accessibility
- **Brand Identity**: Sharp edges, geometric shapes, technical sophistication
- **Professionalism**: Code artists who design with vision, boldness, and precision

## CRITICAL: SCSS File Structure (ALWAYS FOLLOW)
- **NEVER use inline styles in component.ts files**
- **ALWAYS create separate .scss files for each component**
- **File naming**: `component-name.component.scss`
- **Import SCSS in component**: `styleUrls: ['./component-name.component.scss']`
- **Use SCSS features**: Variables, nesting, mixins, functions
- **Organize SCSS**: Base, layout, components, utilities sections

## Color System (NEVER CHANGE)
### Primary Colors:
- **White (#FFFFFF)** – Simplicity, trust, minimalism
- **Green (#10b981)** – Innovation, growth, personality

### Secondary Colors:
- **Light Green (#a7f3d0)** – Energy, freshness
- **Teal (#0d9488)** – Balance, subtlety, modern feel

### Optional Additions (for contrast/UI states):
- **Dark Charcoal (#111827)** – For footer backgrounds or dark mode
- **Muted Grey (#6b7280)** – For subtitles or muted interface elements

## Typography System (NEVER CHANGE)
### Primary Font: Code Bold
- **Logo**: Semi-Bold | Clear and bold, not heavy
- **Hero Headline**: Light/Medium | Large, stylized text
- **Headings**: Bold | Emphasis and hierarchy
- **Subheadings**: Medium | Structural clarity
- **Navigation**: Medium | Crisp and legible
- **Buttons/CTAs**: Medium/Semi-Bold | Action-driven clarity

### Secondary Font: Coolvetica
- **Body Text**: Regular | Clean readability
- **Form Labels**: Regular | Clean and adaptable
- **Footers**: Regular | Used with lowercase texts

### Typography Scale:
- Hero: 3rem - 4rem (Code Bold, Light/Medium)
- H1: 2.5rem - 3rem (Code Bold, Bold)
- H2: 2rem - 2.5rem (Code Bold, Bold)
- H3: 1.5rem - 2rem (Code Bold, Medium)
- Body: 1rem - 1.125rem (Coolvetica, Regular)
- Small: 0.875rem - 1rem (Coolvetica, Regular)

## Brand Visual Elements (ALWAYS INCORPORATE)
### Core Brand Shapes:
- **< / > Code Symbols** – Anchoring our dev identity
- **Triangles and Sharp Angles** – Represent precision and control
- **Squared fusion in p/d** – Symbol of unity and flow inside a structured system
- **Sharp Edges** – Technical sophistication
- **Geometric Patterns** – Structured, controlled design

### Brand Graphics Guidelines:
- Custom, edged icons with technical but friendly tone
- Consistent weight and spacing across platforms
- The "<>" must be present around the website
- Edged and sophisticated lines should be present over the website

## Layout Guidelines
### Modular Design:
- Clean, minimalist sections with sharp edges
- Controlled whitespace between elements
- Natural flow and organization between content
- Connection between content elements

### Hero Section:
- Background with subtle geometric pattern
- Large, stylized Code Bold typography
- Sharp, edged elements
- Clear CTA zones

### Section Headers:
- Badge with "<>" code symbols
- Bold Code Bold titles
- Clean Coolvetica descriptions
- Sharp triangular accents

## SCSS Architecture & Best Practices
### File Structure
```scss
// component-name.component.scss
// ==========================================

// Variables (ALWAYS USE BRAND COLORS)
$primary-green: #10b981;
$teal: #0d9488;
$light-green: #a7f3d0;
$white: #ffffff;
$dark-charcoal: #111827;
$muted-grey: #6b7280;

// Typography Variables
$font-primary: 'Code Bold', monospace;
$font-secondary: 'Coolvetica', sans-serif;

// Responsive Mixin
@mixin responsive($breakpoint) {
  @if $breakpoint == mobile {
    @media (max-width: 767px) { @content; }
  } @else if $breakpoint == tablet {
    @media (min-width: 768px) and (max-width: 1023px) { @content; }
  } @else if $breakpoint == desktop {
    @media (min-width: 1024px) { @content; }
  }
}

// Brand Element Mixins
@mixin sharp-edge {
  border-radius: 0.25rem; // Sharp edges, not rounded
}

@mixin triangle-accent($size: 8px, $color: $primary-green) {
  &::after {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 0;
    height: 0;
    border-left: $size solid transparent;
    border-bottom: $size solid $color;
  }
}

@mixin code-symbols {
  &::before {
    content: '<';
    color: $primary-green;
    margin-right: 0.5rem;
  }
  &::after {
    content: '/>';
    color: $primary-green;
    margin-left: 0.5rem;
  }
}

@mixin geometric-pattern {
  background-image: 
    linear-gradient(45deg, $light-green 25%, transparent 25%),
    linear-gradient(-45deg, $light-green 25%, transparent 25%),
    linear-gradient(45deg, transparent 75%, $light-green 75%),
    linear-gradient(-45deg, transparent 75%, $light-green 75%);
  background-size: 20px 20px;
  background-position: 0 0, 0 10px, 10px -10px, -10px 0px;
}

// SHOWER GLASS EFFECT - Frosted, Opaque, Grey Tone (NO SHADOWS)
@mixin shower-glass-effect {
  background: rgba(128, 128, 128, 0.4); // Increased opacity for more visible effect
  backdrop-filter: blur(25px) saturate(250%); // Increased blur and saturation for better glass effect
  -webkit-backdrop-filter: blur(25px) saturate(250%);
  border: 1px solid rgba(255, 255, 255, 0.5); // More visible border
  position: relative; // Ensure proper stacking context
  z-index: 5; // Higher than floating shapes
  
  // Additional frosted layer for more opacity
  &::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(200, 200, 200, 0.2); // Increased opacity
    backdrop-filter: blur(10px); // Increased blur
    -webkit-backdrop-filter: blur(10px);
    border-radius: inherit;
    z-index: -1;
  }
}
```

### Component Design Rules

### Buttons
```scss
@mixin btn--primary {
  background: $primary-green;
  color: $white;
  font-family: $font-primary;
  font-weight: 600;
  padding: 0.75rem 1.5rem;
  border: none;
  @include sharp-edge;
  @include code-symbols;
  transition: all 0.3s ease-in-out;
  
  &:hover {
    background: $teal;
    transform: translateY(-2px);
  }
}

@mixin btn--secondary {
  background: transparent;
  color: $primary-green;
  font-family: $font-primary;
  font-weight: 600;
  padding: 0.75rem 1.5rem;
  border: 2px solid $primary-green;
  @include sharp-edge;
  transition: all 0.3s ease-in-out;
  
  &:hover {
    background: $primary-green;
    color: $white;
  }
}
```

### Titles
```scss
@mixin title--hero {
  font-family: $font-primary;
  font-weight: 300; // Light/Medium for hero
  font-size: 3rem;
  color: $dark-charcoal;
  line-height: 1.2;
  @include triangle-accent(12px, $primary-green);
  
  @include responsive(mobile) {
    font-size: 2rem;
  }
}

@mixin title--section {
  font-family: $font-primary;
  font-weight: 700; // Bold for sections
  font-size: 2.5rem;
  color: $dark-charcoal;
  line-height: 1.3;
  @include triangle-accent(8px, $primary-green);
  
  @include responsive(mobile) {
    font-size: 2rem;
  }
}
```

### Boxes/Cards
```scss
@mixin card--primary {
  background: $white;
  border: 1px solid rgba($primary-green, 0.1);
  @include sharp-edge;
  @include code-symbols;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
  padding: 1.5rem;
  transition: all 0.3s ease-in-out;
  
  &:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
  }
}
```

## Responsive Breakpoints
```scss
// Mobile-first approach
$mobile: 767px;
$tablet: 768px;
$desktop: 1024px;
$large-desktop: 1440px;

// Usage with mixin
@include responsive(mobile) { /* styles */ }
@include responsive(tablet) { /* styles */ }
@include responsive(desktop) { /* styles */ }
```

## Animation Classes
```scss
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.9);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.animate-fade-up {
  animation: fadeInUp 0.6s ease-out forwards;
}

.animate-scale-in {
  animation: scaleIn 0.4s ease-out forwards;
}
```

## Implementation Checklist
- [ ] Use semantic HTML structure
- [ ] Create separate SCSS file for component
- [ ] Apply consistent spacing system
- [ ] Implement responsive breakpoints
- [ ] Add smooth animations to all interactive elements
- [ ] Ensure accessibility compliance
- [ ] Test across all device sizes
- [ ] Optimize for performance
- [ ] Follow BEM naming convention
- [ ] Use CSS custom properties for theming
- [ ] Implement proper focus states
- [ ] Use correct font families (Code Bold for titles, Coolvetica for body)
- [ ] Apply brand color palette consistently
- [ ] Use SCSS variables and mixins
- [ ] Incorporate brand shapes (triangles, sharp edges, code symbols)
- [ ] Add geometric patterns where appropriate
- [ ] Maintain harmony between sharp and smooth elements
- [ ] Include "<>" code symbols in appropriate places

## Reference Standards
- **ALWAYS** use brand color palette exactly as specified
- **ALWAYS** incorporate brand visual elements with harmony
- **ALWAYS** maintain sharp, technical aesthetic
- **ALWAYS** use Code Bold for headings and CTAs
- **ALWAYS** use Coolvetica for body text
- **NEVER** use rounded corners (except where specified)
- **NEVER** use font-weight: bold (use Code Bold font instead)
- **ALWAYS** include "<>" code symbols in appropriate places
- **ALWAYS** create separate SCSS files for components 

    Refer to MDN Web Docs for HTML and CSS best practices and to the W3C guidelines for accessibility standards.
    